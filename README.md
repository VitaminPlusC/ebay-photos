# ebay-photos

Static image hosting for eBay listings. Photos are referenced from listing CSVs via `raw.githubusercontent.com` URLs and fetched by eBay when listings publish.

URL pattern: `https://raw.githubusercontent.com/VitaminPlusC/ebay-photos/main/<folder>/<filename>` (spaces become `%20`).
